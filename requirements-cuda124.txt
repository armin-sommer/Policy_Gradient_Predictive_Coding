# CUDA 12.4 runtime pin — install AFTER requirements-gpu.txt with:
#     pip install --force-reinstall --no-deps -r requirements-cuda124.txt
#
# Why: jaxlib 0.4.38 is built against CUDA 12.4, but pip resolves the nvidia-*
# wheels to 12.8/12.9, which segfault on the first matmul ("INTERNAL: the
# library was not initialized" / "Failed to capture gpu graph"). Forcing the
# 12.4 set is the fix. --no-deps is required so pip does not re-resolve them
# back up. This breaks torch's CUDA, which these experiments do not use.
nvidia-cublas-cu12==12.4.5.8
nvidia-cuda-cupti-cu12==12.4.127
nvidia-cuda-nvrtc-cu12==12.4.127
nvidia-cuda-runtime-cu12==12.4.127
nvidia-cudnn-cu12==9.1.0.70
nvidia-cufft-cu12==11.2.1.3
nvidia-cusolver-cu12==11.6.1.9
nvidia-cusparse-cu12==12.3.1.170
nvidia-nccl-cu12==2.21.5
nvidia-nvjitlink-cu12==12.4.127
