#!/bin/sh
# Strip agent Co-authored-by trailers before the commit lands on GitHub.
msg="$1"
[ -f "$msg" ] || exit 0
tmp="${msg}.$$"
grep -viE '^(co-authored-by:.*(cursor|claude|copilot|anthropic|openai|codex|composer)|made.with:?[[:space:]]*cursor)' "$msg" > "$tmp" && mv "$tmp" "$msg"
